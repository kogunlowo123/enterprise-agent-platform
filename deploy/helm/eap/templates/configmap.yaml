apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "eap.fullname" . }}-config
  labels:
    {{- include "eap.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}