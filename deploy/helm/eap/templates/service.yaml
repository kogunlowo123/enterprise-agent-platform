apiVersion: v1
kind: Service
metadata:
  name: {{ include "eap.fullname" . }}
  labels:
    {{- include "eap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eap.selectorLabels" . | nindent 4 }}